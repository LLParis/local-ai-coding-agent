{
  "schema": "coding-intelligence.research-radar.sync-config/v1",
  "categories": ["cs.AI", "cs.CL", "cs.SE"],
  "discovery_terms": [
    "agent evaluation",
    "agent harness",
    "agent memory",
    "agent orchestration",
    "automated research",
    "coding agent",
    "compaction",
    "context management",
    "local inference",
    "local language model",
    "long context",
    "model routing",
    "multi-agent",
    "secure tool use",
    "self-improvement",
    "software engineering agent",
    "speculative decoding",
    "tool calling",
    "tool security"
  ],
  "triage": {
    "profile_id": "coding-intelligence-command-center-v1",
    "active_questions": {
      "durable_memory": ["agent memory", "persistent memory", "compaction", "context management"],
      "harness_tooling": ["agent harness", "tool calling", "coding agent", "software engineering agent"],
      "local_model_routing": ["model routing", "expert routing", "mixture of experts", "small language model"],
      "evaluation_integrity": ["agent evaluation", "evaluator", "held-out", "benchmark leakage"],
      "governed_autoresearch": ["automated research", "self-improvement", "experiment selection"]
    },
    "observed_failures": {
      "tool_loop": ["tool loop", "tool-loop", "invalid tool call"],
      "context_loss": ["context loss", "long context", "compaction loss"],
      "stale_evaluator": ["stale evaluator", "evaluator bias", "benchmark contamination"],
      "lifecycle_failure": ["process lifecycle", "crash recovery", "cross-host"]
    },
    "mutable_layers": {
      "memory": ["memory", "retrieval", "compaction"],
      "harness": ["harness", "tool calling", "agent loop"],
      "router": ["routing", "expert", "orchestration"],
      "evaluator": ["evaluation", "verifier", "benchmark"],
      "inference": ["inference", "kv cache", "speculative decoding"]
    },
    "supported_categories": ["cs.AI", "cs.CL", "cs.LG", "cs.MA", "cs.SE"]
  },
  "budgets": {
    "max_requests": 24,
    "max_response_bytes": 16777216,
    "max_items": 40,
    "max_discovery_items": 30,
    "max_frontier_items": 20,
    "max_backlog_items": 10,
    "max_recheck_items": 10,
    "max_artifacts": 8,
    "timeout_seconds": 30,
    "max_wall_seconds": 180,
    "retry_count": 0
  },
  "cadence": {
    "discovery_lookback_hours": 48,
    "version_recheck_days": 7
  },
  "verify_artifacts": true
}
